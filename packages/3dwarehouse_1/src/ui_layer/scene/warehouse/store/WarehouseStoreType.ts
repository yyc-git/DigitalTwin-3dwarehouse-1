import { mode } from "../../../../scene3d_layer/script/scene/scene_warehouse/type/StateType"

export type store = {
    mode: mode,
    cabinetDrawerAnimationIsPlaying: boolean,
    // isShowModal: boolean,
    currentSceneIndex:number
}